/// @description Init

if(global.pipeCount == 0 || global.pipeCount == 2)
{
	image_speed = 0;	
}
else
{
	image_speed = 1;
}

if(global.pipeCount == 3)
{
	image_xscale = -1;	
}
playerJustSpawned = false;